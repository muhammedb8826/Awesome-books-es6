const ShowAllBooks = (bookTitle, bookAuthor) => `<div class="books">
    <p id="title">"${bookTitle}" by ${bookAuthor}</p>
    <button class="remove">Remove</button></div>`;

export default ShowAllBooks;